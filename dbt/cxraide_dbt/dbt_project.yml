name: cxraide_dbt
version: "0.1.0"
config-version: 2

profile: cxraide_dbt

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

models:
  cxraide_dbt:
    staging:
      +materialized: view
    marts:
      +materialized: table

